import React from "react";
import ProjectDetails from "./ProjectDetails";

const ProjectDetailsWindow = ({ project }) => {
  return (
    <ProjectDetails project={project} />
  );
};

export default ProjectDetailsWindow;